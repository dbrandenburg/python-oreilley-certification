#!/usr/bin/env python3

from tkinter import *

ALL = N+S+W+E


f = Frame(self)
f.grid(row=0, column=1, rowspan=5, columnspan=4, sticky=ALL)
root = Tk()
app = Application(master=root)                
app.mainloop()